import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
  darkMode: 'class', // Habilita o modo escuro baseado em classe (não segue sistema automaticamente)
};
export default config;
